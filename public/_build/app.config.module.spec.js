describe('app.config module', function(){

  var module;

  beforeEach(function() {
    module = angular.module('app.config');
  });

  it('should exist', function(){
    expect(module).to.be.ok;
  });

});